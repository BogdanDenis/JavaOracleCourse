import React from 'react';

import './logo.less';

export const Logo = () => (
  <section className="logo">
    <img
      src="images/logo/logo.svg"
      alt="IT"
      className="icon"
    />
  </section>
);
